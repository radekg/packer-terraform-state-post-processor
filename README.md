[Code compiles but does not work](https://github.com/hashicorp/packer/issues/7502).

```
"post-processors": [
    {
        "type": "terraform-state",
        "terraform_file_path": "...",
    }
]
```